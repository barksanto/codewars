// Write a function that accepts two parameters (sum and multiply)
// and find two numbers[x, y], where x + y = sum and x * y = multiply.

const numberOfArgs = (args) => {
  console.log(args.length)
}

numberOfArgs([12, 36])
